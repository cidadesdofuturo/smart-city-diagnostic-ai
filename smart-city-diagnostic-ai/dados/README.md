# Pasta de dados local

Coloque aqui `indicadores.xlsx` apenas para execução local, se usar o caminho padrão `./dados`.

A planilha, os índices FAISS e os relatórios gerados são ignorados pelo Git. Esta pasta permanece no repositório apenas para documentar a estrutura esperada.
